# ERA — Standalone local mode (NO Docker required)
# Auto-setup: .\scripts\setup-keys.ps1  or  GO.bat

ERA_STANDALONE=true
ERA_DEMO_MODE=true

OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
ERA_SERVER_SALT=dev-salt-change-me-in-production

# SQLite: backend/era_standalone.db
# Celery: in-process (no Redis, no worker)

# Production keys (Neon, Upstash, CORS, VITE_API_URL): DEPLOY.bat
